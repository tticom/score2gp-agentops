Do not merge yet.

Fix:
1. enforce stable pitch offset across the entire duplicate pair
2. choose notation/TAB roles by staff evidence, not min/max voice number
3. add tests for both cases
4. clean the PR body formatting

Then re-run full tests and private smoke. After that, this should be mergeable.
