# CR-04D3 Promotion

Product PR #387 was squash-merged as `36b3016...`. Fresh product `origin/main` contains `pdf_tab_measure_timing.py` and `pdf_tab_event_factory.py`.

Source revalidation confirmed a cohesive per-source-bar assembly boundary remains inside `build_ir_from_tabraw_only()`. Prompt 0014 is promoted to active status with explicit allowlist and characterization test requirements. Prompts 0015-0016 remain inactive wireframes.

No product changes were made by this governance promotion action.
