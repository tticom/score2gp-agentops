# Antigravity Task: GPIF Slur Parse Recovery Minimal v0.1

## Role

You are the Developer.

Implement minimal GPIF parse recovery for slurs if the existing writer already serializes slurs and ScoreIR already represents them.

This branch is about GPIF parse recovery only. Do not implement PDF vector slur detection.

## Dependency

Proceed only after the previous technique PRs are reviewed and merged, unless explicitly instructed otherwise.

Start from clean main:

```bash
git switch main
git pull --ff-only origin main
git switch -c feature/gpif-slur-parse-recovery-minimal-v0.1
```
