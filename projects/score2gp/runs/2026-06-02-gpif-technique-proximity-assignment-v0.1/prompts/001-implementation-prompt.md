# Antigravity Task: PDF Technique X-Proximity Assignment v0.1

## Role

You are the Developer.

Improve PDF-to-ScoreIR technique attachment by replacing or supplementing naive count-based technique assignment with visual x-proximity alignment for already-extracted technique text candidates.

This branch should focus on hammer-on, pull-off, and slide first. Do not attempt every technique.

## Dependency

Proceed only after `feature/gpif-hammer-pull-slide-minimal-v0.1` is merged into main.

Before starting:

```bash
git switch main
git pull --ff-only origin main
git log --oneline --decorate --max-count=10
```
