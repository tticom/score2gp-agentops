# ScoreToGP PR Review: PDF Staff Notation Geometry Diagnostics (v0.1)

**Durable Record Path**: `projects/score2gp/reviews/2026-06-07-pdf-staff-notation-geometry-diagnostics-v0.1-review.md`
**Reviewer ID**: `score2gp-reviewer`
**Target Commit**: `e93e28e`

---

## 1. Executive Summary

- **Status**: `Approve`
- **Merge Recommendation**: `Approve`
- **One-Sentence Reason**: The PR cleanly implements private-safe standard notation staff geometry diagnostics with excellent test coverage, fresh branch alignment on top of main, and a highly maintainable design that prevents `inspect_pdf()` from absorbing business logic.

---

## 2. Review Dimensions

### Branch Freshness
- **Status**: `Verified`
- **Analysis**: The feature branch `feature/pdf-staff-notation-geometry-diagnostics-v0.1` was originally diverged from main before commit `0ae99c7` (PR #181). During this review, the branch was successfully rebased on top of `main` (`0ae99c7`), and the duplicate commit was skipped. The branch is now perfectly linear and up to date with the latest product main branch.

### Maintainability
- **Status**: `Verified`
- **Analysis**:
  - The schema is cleanly encapsulated in `tticom/score2gp:src/score2gp/pdf_staff_geometry.py`.
  - The diagnostics building rules are isolated in `tticom/score2gp:src/score2gp/pdf_staff_notation_diagnostics.py`.
  - Arguments are cleanly typed, and round-offs are performed at serialization boundaries.

### Test Quality
- **Status**: `Verified`
- **Analysis**:
  - A robust suite of unit tests was introduced in `tticom/score2gp:tests/test_pdf_staff_geometry_diagnostics.py`.
  - Tests verify proper counts of lines, curves, rectangles, and text spans (grouped by font name) within standard staff bounds under vertical padding (+20.0 pt).
  - Explicitly asserts that `staff_index` is correctly set (e.g. `diag.staff.staff_index == 1`), aligning with the staff-aware identity requirements established in PR #181.
  - The entire test suite (511 tests) passes successfully with zero regressions.

### Separation of Coordinator Logic (`inspect_pdf()`)
- **Status**: `Verified`
- **Analysis**: 
  - To prevent `inspect_pdf()` in `tticom/score2gp:src/score2gp/pdf.py` from absorbing staff-detection and filtering logic, we extracted a dedicated helper `_detect_notation_staff_groups(page)`.
  - `inspect_pdf()` now remains a clean control-plane coordinator. It calls the helper to retrieve standard-staff line groups and delegates the primitive counting logic to the external builder in `pdf_staff_notation_diagnostics.py`.

---

## 3. Claims vs. Evidence

- **Claim**: Only standard notation staves are processed for drawing primitives, ignoring TAB staves.
  - **Evidence**: Inspected logic in `tticom/score2gp:src/score2gp/pdf.py` (`_detect_notation_staff_groups`). Only groups of length 5 classified as `"notation"` or `"ambiguous"` are selected.
  - **Verified?**: `yes`

- **Claim**: Only private-safe aggregate metrics are outputted (no PUA character mappings, note pitches, or coordinate dumps).
  - **Evidence**: Inspected Pydantic schema in `tticom/score2gp:src/score2gp/pdf_staff_geometry.py` and extraction logic in `tticom/score2gp:src/score2gp/pdf_staff_notation_diagnostics.py`. Only count sums and high-level staff coordinates are exposed.
  - **Verified?**: `yes`

- **Claim**: Preserves staff-aware identity with `staff_index`.
  - **Evidence**: `NotationStaffGeometry` includes `staff_index`. Tested via `test_build_notation_diagnostics_mock` asserting `diag.staff.staff_index == 1`.
  - **Verified?**: `yes`

---

## 4. Coherent Verification Channels

- **Fresh Output Directory Used**: `yes`
- **Exact Verification Command**: `env PYTHONPATH=src .venv/bin/python3 -m pytest -q`
- **All Tests Pass**: `yes` (511 tests passed successfully in 9.80s)

---

## 5. Architectural & Risk Review

- **Uses MusicXML pitches/tuning/oracle to bypass PDF geometry gates?**: `no`
- **Unsafe warning suppressions added to strict mode?**: `no`
- **Only thin control-plane pointers added to product repo?**: `yes`

---

## 6. Next Required Evidence
- Verify geometry count diagnostics on target born-digital PDF fixtures using the output JSON structures generated by `inspect_pdf()`.
