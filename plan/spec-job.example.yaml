schema_version: 1
job:
  id: JOB-001
  title: Example implementation job
  spec: docs/specs/example.md
  repository: tticom/score2gp
  base_branch: main
  integration_branch: codex/job-001
tickets:
  - id: T-001
    title: First vertical slice
    objective: Describe the user-visible behaviour delivered by this slice.
    status: READY
    blocked_by: []
    allowed_paths:
      - src/example/**
      - tests/example/**
    acceptance:
      - The first slice is observable through the normal product entrypoint.
    validation:
      - python3 -m pytest tests/example
  - id: T-002
    title: Dependent vertical slice
    objective: Describe the next user-visible behaviour delivered by this slice.
    status: READY
    blocked_by:
      - T-001
    allowed_paths:
      - src/example/**
      - tests/example/**
    acceptance:
      - The dependent slice is observable through the normal product entrypoint.
    validation:
      - python3 -m pytest tests/example
